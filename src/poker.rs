pub fn deal(perm: [u32; 9]) -> Vec<String> {
  // create a deck from perm values
  // deal out two players
  // determine winning hand from each player, use best_hand
  // compare the two, pick winning hand
  // return winning hand .to_str_vec()
  Vec::new()
}
